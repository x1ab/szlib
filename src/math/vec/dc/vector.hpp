//============================================================================
// API for vectors using the native built-in data representation (VectorData)
//============================================================================

#include "_vector_core-dc.hpp"
#include "../data.hpp" // Needed only for native vectors (not for adapters).
