//!![[AD(H)D-Driven-Development (AD(H)DDD, or ADDDD, for short... ;) )]]
//!! vocab headers can't include other vocab headers, so an umbrella aggregate
//!! would need to be created according to some not-yet-defined principle(s)!
